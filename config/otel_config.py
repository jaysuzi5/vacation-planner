"""
OpenTelemetry is configured via environment variables and the
opentelemetry-instrument CLI wrapper in the Dockerfile CMD.
This module exists as a hook for any custom span processors if needed later.
"""
